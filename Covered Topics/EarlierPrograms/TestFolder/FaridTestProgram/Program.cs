using System;       // imports libraries for using the classes necessary to use the console.

namespace Farid     // namespace used to group classes, modules, etc, to avoid the naming conflicts.
{
    class MyClass
    {
        static void Main(string[] args)     //main entry point the main method of a C# Program
        {
            Console.WriteLine("Hello, World!");
            const int daysInWeek = 7;
            Console.WriteLine(daysInWeek);
        }
    }
}

